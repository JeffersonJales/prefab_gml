// Script Desc 
function button_show_debug_message(text){
	var _id = string(id);
	show_debug_message(_id + " : " + text)
}

function button_show_message(text){
	var _id = string(id);
	show_message(_id + " : " + text)
}